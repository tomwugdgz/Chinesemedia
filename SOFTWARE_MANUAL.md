# 社区户外媒体点位资源与投放排期协同管理系统
# MediaPlaner — 软件产品说明书

**文档版本:** v2.5.0 (Revised)　|　**对应软件版本:** Release v2.5.0 (Enterprise Edition)
**修订日期:** 2026-08-20　|　**维护团队:** MediaPlaner 研发中心
**修订依据:** v2.4.0 技术评审意见(P0/P1/P2 全量闭环)

---

## 修订记录 (Change Log)

| 章节 | v2.4.0 | v2.5.0 修订内容 | 级别 |
|---|---|---|---|
| 1 | 纯前端持久化 | **重构为服务端权威架构**, PostgreSQL 为唯一事实来源, 客户端降级为离线缓存 | P0 |
| 2 | 8 大模块 | 扩展为 **11 大模块**, 新增排期日历、供应商合同、工单派单、通知中心、安全权限 | P2 |
| 3 | 单向 4 态直线 | 重构为 **Booking 档期状态机 + Point 物理状态机 + Plan 商务状态机** 三层模型, 补全逆向与异常迁移 | P0 |
| 4 | Point 挂载 status | **引入 Booking / PlanItem 实体**, Point.status 改为派生; 新增 12 个实体与通用审计字段 | P0 |
| 5 | 4 个接口 | 扩展为 **9 大类 60+ 接口**, 新增认证、CRUD、同步、错误码字典、幂等规范 | P1 |
| 6 | 描述性指标 | 重写为**可测量、可验收**口径; 修正 PDF/坐标系/存储上限表述 | P1 |
| 7 | 无 | **新增「安全与权限」独立章节**(RBAC、数据权限、审计、PII 合规) | P1 |
| 附录 | 无 | 新增指标口径表、权限矩阵、术语表、验收标准、刊期对齐与施工缓冲期规范 | P2 |

---

## 1. 软件基本信息与架构定义

### 1.1 基本信息

| 属性 | 说明 |
|---|---|
| 软件全称 | 社区户外媒体点位资源与投放排期协同管理系统 (MediaPlaner) |
| 软件版本 | Release v2.5.0 (Enterprise Edition) |
| 应用架构 | **前后端分离 + 服务端权威 (Server-Authoritative)**: React 18 SPA (Vite) / Node.js 20 Express API / PostgreSQL 16 / Redis 7 / 对象存储 (OSS) |
| 定位与目标 | 打造集**点位资产数字化、空间地理选点、档期排他风控、AI 智能媒介规划、外勤工程留证**于一体的户外传媒运营中台 |
| 核心能力锚点 | 以**「档期 (Booking)」为最小售卖单元**, 通过数据库级排他约束物理杜绝同一点位同一时段的双重售卖 |

### 1.2 架构总览

```
┌──────────────────────────────────────────────────────────────────┐
│  客户端层  React 18 + Vite + TypeScript + TanStack Query          │
│  ├─ 视图: 大盘 / 点位 / 地图 / 排期日历 / 方案 / CRM / 巡检 / 设置    │
│  ├─ 状态: Zustand (UI 态) + TanStack Query (服务端态缓存)          │
│  ├─ 离线: IndexedDB (Dexie) 作【只读缓存 + 草稿队列】               │
│  └─ 计算: Web Worker 承载万级点位过滤/排序/聚合                     │
└───────────────┬──────────────────────────────────────────────────┘
                │ HTTPS / REST + SSE(AI 流式) + WebSocket(锁位广播)
┌───────────────▼──────────────────────────────────────────────────┐
│  接入层  Nginx (TLS 终止 / 静态资源 / 限流 / GZip)                  │
└───────────────┬──────────────────────────────────────────────────┘
┌───────────────▼──────────────────────────────────────────────────┐
│  应用层  Node.js 20 + Express 4 + TypeScript                      │
│  ├─ 认证鉴权     JWT(AccessToken 2h / RefreshToken 14d) + RBAC     │
│  ├─ 业务服务     PointSvc / BookingSvc / PlanSvc / CrmSvc / FieldSvc│
│  ├─ 【锁位事务】 悲观锁 + DB 排他约束 + 幂等键                       │
│  ├─ AI 网关      Provider 适配层(Gemini / 可插拔) + 校验与降级       │
│  ├─ 定时任务     node-cron: 锁位到期释放 / 上画提醒 / 合同到期扫描     │
│  └─ 审计切面     全量写操作落 AuditLog                              │
└───┬────────────────┬───────────────┬──────────────┬───────────────┘
    │                │               │              │
┌───▼──────┐  ┌──────▼─────┐  ┌──────▼──────┐  ┌───▼──────────────┐
│PostgreSQL│  │  Redis 7   │  │ OSS / S3    │  │ 外部服务          │
│ 16 (权威)│  │ 锁/队列/缓存│  │ 照片/语音/PDF│  │ 高德地图 / LLM    │
│ + PostGIS│  │ TTL 倒计时 │  │             │  │ 企业微信/钉钉/短信 │
└──────────┘  └────────────┘  └─────────────┘  └──────────────────┘
```

### 1.3 架构原则(强制约束)

| # | 原则 | 说明 |
|---|---|---|
| A1 | **单一事实来源 (SSOT)** | 所有业务数据以 PostgreSQL 为准。客户端存储**仅为缓存与离线草稿**, 不具备权威性 |
| A2 | **强一致操作必须在线** | 锁位、发布、释放、审批等涉及排他性的操作**禁止离线执行**, 断网时按钮置灰并提示 |
| A3 | **排他性下沉至数据库** | 档期冲突由 PostgreSQL `EXCLUDE USING gist` 约束物理保证, 应用层校验仅作为友好提示, 不作为唯一防线 |
| A4 | **状态即派生** | `Point.status` 不落库为可写字段, 由覆盖查询日的 Booking 实时派生 (物化视图加速) |
| A5 | **一切变更可追溯** | 状态迁移写 `StatusTransitionLog`, 全量写操作写 `AuditLog`, 均不可删除、不可篡改 |
| A6 | **AI 输出不可信** | LLM 返回结果必须经服务端二次校验 (ID 存在性 + 档期可用性 + 预算约束) 后方可返回前端 |

---

## 2. 软件功能结构与模块清单

```
MediaPlaner v2.5.0 功能树
├── 1. 运营大盘与数据监控 (Dashboard)
│   ├── 1.1 核心经营指标概览 (在载率/可售率/总点位数/覆盖户数/活跃方案)
│   ├── 1.2 点位实时状态分布环形图 (可选/已选/已锁/在播/维护中/停用)
│   ├── 1.3 30 天库存周转与状态流转折线图 [数据源: StatusTransitionLog]
│   ├── 1.4 待办业务提醒中心 (锁位超期/待上画/待巡检/合同到期/审批)
│   ├── 1.5 A4 规范级 PDF 库存周报生成与导出 (服务端 Puppeteer / 前端备用)
│   └── 1.6 数据看板按数据权限自动裁剪 (仅见本人/本团队/全公司)
│
├── 2. 点位资产全生命周期管理 (Point Management)
│   ├── 2.1 状态快速筛选栏 (全部/可选/已选/已锁/在播/维护中/停用)
│   ├── 2.2 全局多维模糊搜索 (小区/地址/编号/客户/方案/供应商)
│   ├── 2.3 多维组合过滤器 (城市/区域/商圈/级别/媒体类型/去重簇/【可用档期】)
│   ├── 2.4 列表 Table / 卡片 Grid / 地图 Map 三视图切换
│   ├── 2.5 批量选点与多选加入投放方案 (自动档期冲突预检)
│   ├── 2.6 点位台账批量导入导出 (CSV/JSON, 异步任务 + 校验报告)
│   ├── 2.7 点位详情与全量参数维护
│   ├── 2.8 【新增】点位档期时间轴 (过去/当前/未来全部 Booking)
│   ├── 2.9 【新增】刊例价历史版本与调价记录
│   └── 2.10 【新增】去重簇管理 (dedupCluster, 跨来源重复点位归并)
│
├── 3. 空间地理地图选点与可视化 (GIS Map View)
│   ├── 3.1 高德底图 (坐标系统一 GCJ-02)
│   ├── 3.2 状态高亮图标与多色标记 + 海量点聚合 (Loca/MassMarks)
│   ├── 3.3 气泡交互浮窗与一键选点加入方案
│   ├── 3.4 城市商圈与行政区域快速定位
│   ├── 3.5 【新增】围栏/半径圈选批量入池 (PostGIS ST_DWithin)
│   └── 3.6 【新增】按指定档期区间着色 (该时段可售/占用)
│
├── 4. 【新增】排期日历与档期视图 (Schedule Calendar) ★ 户外媒体核心刚需
│   ├── 4.1 点位维度甘特图 (行=点位, 列=时间, 块=Booking)
│   ├── 4.2 方案维度甘特图 (行=方案, 展示其全部点位档期)
│   ├── 4.3 档期可用性查询 ("2026-11-01~11-30 哪些 A 级点位可售")
│   ├── 4.4 拖拽调整档期 (受权限与冲突校验约束)
│   └── 4.5 空窗期 (Idle Gap) 识别与促销位推荐
│
├── 5. AI 智能媒介策划引擎 (AI Smart Planner)
│   ├── 5.1 客户画像特征深度计算
│   ├── 5.2 智能推荐点位池 (匹配得分 0-100 / 契合原因 / CPM 预估)
│   ├── 5.3 自动化投放策略企划案生成 (文案创意/排期建议/配比建议)
│   ├── 5.4 AI 媒介顾问对话助手 (SSE 流式)
│   ├── 5.5 【新增】推荐结果服务端二次校验 (存在性/档期/预算)
│   └── 5.6 【新增】Token 消耗审计与用户配额管理
│
├── 6. 投放计划与排期商务流转 (Plan Management)
│   ├── 6.1 方案创建/编辑/预算与刊例自动核算 (价格快照 + 折扣 + 制作费 + 税)
│   ├── 6.2 方案状态机流转 (草稿→选点中→待审→已锁→已发布→已结案)
│   ├── 6.3 锁位排他保护期计时器 (分档可配置, 服务端 cron 自动释放)
│   ├── 6.4 档期冲突与排他性检测 (区间重叠 + 客户独占规则)
│   ├── 6.5 打印级《上画通知书》生成 (含公章与施工验收标准)
│   ├── 6.6 【新增】PlanItem 明细管理 (单点单价/单点上画状态/单点巡检结果)
│   ├── 6.7 【新增】报价单 PDF 与合同附件挂载
│   └── 6.8 【新增】审批流 (提交/驳回/通过, 含驳回原因)
│
├── 7. 客户关系管理 (CRM)
│   ├── 7.1 客户档案库 (行业/品牌/联系方式/负责销售)
│   ├── 7.2 客户等级划分 (A 类战略 / B 类重点 / C 类普通)
│   ├── 7.3 【重构】独占保护机制 → 可配置 ProtectionRule (维度/时长/优先级)
│   ├── 7.4 客户拜访跟进记录流水账
│   └── 7.5 【新增】客户历史投放画像 (累计金额/偏好区域/偏好级别)
│
├── 8. 【新增】供应商与点位来源管理 (Supplier & Contract)
│   ├── 8.1 供应商 (物业/媒体主) 档案
│   ├── 8.2 点位采购合同与授权期限
│   ├── 8.3 合同到期预警 → 点位自动转「已停用」
│   └── 8.4 采购成本与毛利测算
│
├── 9. 外勤工程巡检与现场核销 (Field Inspection)
│   ├── 9.1 上画拍照与实景留证 (时间戳 + 定位水印, 原图存 OSS)
│   ├── 9.2 四项质量核检清单 (外框完好/画面平整/照明正常/无遮挡)
│   ├── 9.3 现场语音便签录制与回放
│   ├── 9.4 异常点位报修 → 点位转「维护中」+ 生成维修工单
│   ├── 9.5 【新增】工单派单闭环 (待派→已派→进行中→完工→验收/退回)
│   └── 9.6 【新增】离线巡检采集与联网自动同步
│
├── 10. 【新增】消息与通知中心 (Notification)
│   ├── 10.1 站内信与待办聚合
│   ├── 10.2 企业微信 / 钉钉 / 短信 多通道推送
│   └── 10.3 通知模板与订阅策略配置
│
└── 11. 系统设置、安全与数据管理 (Settings, Security & Data)
    ├── 11.1 业务规则参数配置 (锁位期限分档 / 折扣底线 / 独占规则)
    ├── 11.2 【新增】用户、角色与 RBAC 权限矩阵
    ├── 11.3 【新增】数据权限范围配置 (本人/本团队/本城市/全公司)
    ├── 11.4 【新增】操作审计日志查询与导出
    ├── 11.5 离线缓存策略与同步冲突裁决队列
    ├── 11.6 全量数据备份、导出 JSON 与灾难恢复 (服务端定时 + 手动)
    └── 11.7 演示初始数据一键重置 (仅演示环境可见)
```

---

## 3. 核心业务流程与状态机模型

### 3.1 领域模型总览

```
Supplier ──1:N── SupplierContract ──1:N── Point
                                            │
                                            │ 1:N
                                            ▼
Customer ──1:N── Plan ──1:N── PlanItem ──1:1── Booking ◄── 【档期 = 最小售卖单元】
   │              │                                │
   │ 1:N          │ 1:N                            │ 1:N
   ▼              ▼                                ▼
ProtectionRule  Approval                    StatusTransitionLog
                                                   
Point ──1:N── MediaAsset(照片/语音)
Point ──1:N── Inspection ──1:N── WorkOrder
Point ──1:N── PriceHistory
```

### 3.2 档期状态机 (Booking Status Machine) ★ 核心

```
                        ┌──────────────────────┐
                        │  (无 Booking = 可选)  │
                        └───────────┬──────────┘
                                    │ ① 加入方案(选定档期区间)
                                    ▼
                        ┌──────────────────────┐
              ┌─────────│   SELECTED 已选       │
              │         └───────────┬──────────┘
              │ ② 移出方案/方案删除   │ ③ 提交锁位申请(客户+倒计时)
              │  → CANCELLED         ▼
              │         ┌──────────────────────┐
              ├─────────│    LOCKED 已锁        │◄────┐
              │         └───────┬──────┬───────┘     │
              │ ④ 锁位超期自动释放│      │             │ ⑦ 审批驳回
              │  → EXPIRED       │      │ ⑤ 合同生效   │  (回退)
              │ ⑤' 商务撤单       │      ▼             │
              │  → CANCELLED     │  ┌───────────────┐ │
              │                  └─►│PUBLISHED 已发布│─┘
              │                     └───────┬───────┘
              │                             │ ⑥ 期满下画 → RELEASED
              │                             │ ⑧ 客户违约提前下画 → TERMINATED
              ▼                             ▼
        ┌──────────────────────────────────────────┐
        │  终态: CANCELLED / EXPIRED / RELEASED /   │
        │        TERMINATED  (释放档期, 不再占用)      │
        └──────────────────────────────────────────┘
```

**状态定义与迁移表**

| 状态 | 语义 | 占用档期 | 计入在载率 |
|---|---|---|---|
| `SELECTED` 已选 | 纳入草稿方案, **软占用**, 不排他 | 否(仅提示) | 否 |
| `LOCKED` 已锁 | 商务达成意向, **硬占用**, 排他保护 | **是** | 否 |
| `PUBLISHED` 已发布 | 合同生效, 画面在播 | **是** | 是 |
| `RELEASED` 已释放 | 投放期满正常下画 (终态) | 否 | 否 |
| `EXPIRED` 已过期 | 锁位保护期到期未签约, 系统自动释放 (终态) | 否 | 否 |
| `CANCELLED` 已取消 | 人工撤单/移出方案 (终态) | 否 | 否 |
| `TERMINATED` 已终止 | 违约提前下画 (终态) | 否 | 否 |

**锁位期限分档 (可配置, 默认值)**

| 楼盘级别 | 默认锁位时长 | 可延期次数 | 单次延期 |
|---|---|---|---|
| A++ | 10 天 | 1 | 5 天 |
| A+ | 7 天 | 1 | 3 天 |
| A | 7 天 | 1 | 3 天 |
| B | 3 天 | 1 | 2 天 |
| C | 3 天 | 0 | — |

---

## 4. 核心数据字典与实体关系 (Data Schema)

### 4.1 点位实体 (Point) 与 档期实体 (Booking)

```ts
interface Point extends AuditableEntity {
  id: string;                    // UUID
  pointNo: string;               // 点位编号 PT-SH-001, 租户内唯一
  mediaType: '电梯框架' | '单元门智能框架' | '电梯电视' | '道闸' | '灯箱';
  supplierId: string;            // 关联 Supplier 实体
  supplierContractId?: string;   // 采购合同
  dedupClusterId?: string;       // 去重簇 ID
  dupStatus: '独占' | '跨来源重复' | '来源内重复';
  city: string; area: string; block: string; project: string;
  building?: string; elevatorNo?: string; address: string;
  lat: number; lng: number;      // GCJ-02 坐标
  level: 'A++' | 'A+' | 'A' | 'B' | 'C';
  households: number; population: number; occupancy?: number;
  totalMedia: number; adSize: string;
  listPrice: number; costPrice?: number;
  lifecycle: 'DRAFT' | 'ACTIVE' | 'MAINTENANCE' | 'RETIRED';
  saleableUntil?: string;
}

interface Booking extends AuditableEntity {
  id: string; bookingNo: string;
  pointId: string; planId: string; planItemId: string; customerId: string;
  startDate: string; endDate: string;
  status: 'SELECTED' | 'LOCKED' | 'PUBLISHED' | 'RELEASED' | 'EXPIRED' | 'CANCELLED' | 'TERMINATED';
  lockedAt?: string; lockExpireAt?: string; lockExtendCount: number;
  unitPriceSnapshot: number; weeks: number; discountRate: number; extraFee: number; finalAmount: number;
  installStatus: 'PENDING' | 'INSTALLED' | 'VERIFIED' | 'ABNORMAL';
}
```

---

## 5. 核心优化与增补规范

### 5.1 刊期对齐与施工缓冲期规范 (Standard Broadcast Cycles & Buffer)
1. **刊期自然对齐 (Standard Broadcast Cycle)**:
   - 社区媒体默认支持「周刊期 (Weekly, 每周一起或每周四起)」与「双周刊期 (Bi-Weekly)」，在排期日历中支持一键吸附对齐，避免产生 1-2 天碎片化空置。
2. **施工与换画缓冲期 (Installation Buffer Time)**:
   - 同一点位相邻两个排期之间，系统默认预留 12-24 小时换画缓冲工期，工单引擎自动根据缓冲期生成派单时效要求。

### 5.2 意向候补队列机制 (Lock Waitlist Queue)
- 当热门 A++/A+ 点位被其他商务锁定（`LOCKED`）时，支持次级销售提交**候补意向单 (Waitlist)**。
- 一旦原锁位因超期（`EXPIRED`）或撤单（`CANCELLED`）释放，系统将按候补时间戳自动通知首位候补销售，享有 24 小时优先锁位权。

---

*文档版本：v2.5.0 (Revised) | 维护团队：MediaPlaner 研发中心*

